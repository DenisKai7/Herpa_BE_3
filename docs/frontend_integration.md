# Integrasi Frontend

Set `NEXT_PUBLIC_API_URL=http://localhost:8000`. API client mengirim Supabase access token sebagai `Authorization: Bearer <token>`. Endpoint lama frontend didukung, sehingga integrasi dapat dilakukan bertahap. Patch contoh tersedia di `frontend_patch/`.
